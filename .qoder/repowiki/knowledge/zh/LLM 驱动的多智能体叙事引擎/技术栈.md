- **目标框架**：.NET Standard 2.0 与 .NET Framework 4.8，确保对 Unity 等游戏引擎的广泛兼容性。
- **通信协议**：自定义 MCP (Model Context Protocol) 变体，用于 LLM 与游戏世界之间的工具调用交互。
- **序列化**：内置轻量级 JSON 解析器 (`JsonParser`, `JsonWriter`) 以减少外部依赖。